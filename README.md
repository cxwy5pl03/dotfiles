# dotfiles

Personal configuration files for zsh, git, and tmux.

## Setup

Clone and run `install.sh` to symlink configs into `$HOME`.

## Contents

- `.zshrc` – shell aliases, prompt, and plugins
- `.gitconfig` – global git settings
- `.tmux.conf` – tmux tweaks

## Notes

See `docs/` for environment-specific details.
